/target/
config/*.toml
.env
exec-plan-*.md
.harness/*
!.harness/guards
!.harness/sg
crates/*/.harness/
**/.harness/drafts/
.claude/
~/.local/
.omx/
docs/artifacts/

# Git worktrees (superpowers skill convention)
.worktrees/
web/node_modules/
web/dist/

# SDK build artifacts (TypeScript bindings)
sdk/typescript/node_modules/
sdk/typescript/dist/
sdk/typescript/bun.lock

# macOS finder metadata
.DS_Store

# stray omp capture files
omp.jsonl
*.jsonl
.vite/

# internal planning notes (kept local, never pushed)
/TASK.md

# local MCP client config — carries a personal cluster URL/token; keep local.
# See the root README "Skills → Connecting the MCP endpoint" for the format.
.mcp.json
