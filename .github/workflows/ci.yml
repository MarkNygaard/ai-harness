name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

jobs:
  # ── Step 1: Detect changed paths ──────────────────────────────────────
  changed:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      server: ${{ steps.filter.outputs.server }}
      agents: ${{ steps.filter.outputs.agents }}
      ci: ${{ steps.filter.outputs.ci }}
      site: ${{ steps.filter.outputs.site }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            rust:
              - 'crates/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            server:
              - 'crates/harness-server/**'
              - 'crates/harness-core/**'
              - 'crates/harness-workflow/**'
            agents:
              - 'crates/harness-agents/**'
              - 'crates/harness-core/**'
            ci:
              - '.github/workflows/**'
            site:
              - 'site/**'
              # The site imports the dashboard's shared theme.
              - 'web/src/styles/theme.css'

  # ── Step 2: Fast checks (always run on Rust changes) ──────────────────
  fmt:
    name: Format
    runs-on: ubuntu-latest
    needs: changed
    if: needs.changed.outputs.rust == 'true' || needs.changed.outputs.ci == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  check:
    name: Check
    runs-on: ubuntu-latest
    needs: changed
    if: needs.changed.outputs.rust == 'true' || needs.changed.outputs.ci == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Build web bundle
        working-directory: web
        run: |
          bun install --frozen-lockfile
          bun run build
      - run: cargo check --workspace --all-targets

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    needs: check
    if: needs.changed.outputs.rust == 'true' || needs.changed.outputs.ci == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Build web bundle
        working-directory: web
        run: |
          bun install --frozen-lockfile
          bun run build
      - run: cargo clippy --workspace --all-targets

  # ── Step 3: Tests (only when relevant crates change) ──────────────────
  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [check, changed]
    if: needs.changed.outputs.rust == 'true' || needs.changed.outputs.ci == 'true'
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: harness_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@nextest
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Build web bundle
        working-directory: web
        run: |
          bun install --frozen-lockfile
          bun run build
      - run: cargo nextest run --workspace
        env:
          HARNESS_DATABASE_URL: postgres://postgres:postgres@localhost:5432/harness_test

  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    needs: changed
    if: needs.changed.outputs.rust == 'true' || needs.changed.outputs.ci == 'true'
    permissions:
      contents: read
      checks: write
    steps:
      - uses: actions/checkout@v4
      - uses: rustsec/audit-check@v2.0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  # ── Public site (landing page + docs) ─────────────────────────────────
  site:
    name: Site
    runs-on: ubuntu-latest
    needs: changed
    if: needs.changed.outputs.site == 'true' || needs.changed.outputs.ci == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - name: Install
        working-directory: site
        run: bun install --frozen-lockfile
      - name: Typecheck
        working-directory: site
        run: bunx tsc --noEmit
      - name: Build
        working-directory: site
        run: bunx next build

  # ── Fan-in gate: single required check for branch protection ──────────
  ci-result:
    name: CI Result
    runs-on: ubuntu-latest
    if: always()
    needs: [changed, fmt, check, clippy, test, audit, site]
    steps:
      - name: Check all jobs
        run: |
          results=(
            "${{ needs.changed.result }}"
            "${{ needs.fmt.result }}"
            "${{ needs.check.result }}"
            "${{ needs.clippy.result }}"
            "${{ needs.test.result }}"
            "${{ needs.audit.result }}"
            "${{ needs.site.result }}"
          )
          for r in "${results[@]}"; do
            if [[ "$r" == "failure" || "$r" == "cancelled" ]]; then
              echo "Job failed or cancelled: $r"
              exit 1
            fi
          done
          echo "All CI jobs passed or were skipped"
