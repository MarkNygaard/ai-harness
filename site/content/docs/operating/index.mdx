---
title: "Reference"
description: "Every setting the server reads, the CLI surface, and the agent CLIs the harness drives."
---

The parts you come back to rather than read once. Getting a harness running for
the first time is [setup](/docs/setup).

<Cards>
  <Card title="Configuration" href="/docs/operating/configuration">
    Where settings come from and in what order, every environment variable the
    server reads, and the ones a run injects into your workflow steps.
  </Card>
  <Card title="CLI reference" href="/docs/operating/cli">
    `serve`, `run`, `status`, `exec`, `linear` and `admin`, with flags and
    defaults.
  </Card>
  <Card title="Agents and models" href="/docs/operating/agents">
    The agent CLIs the harness drives, how a node picks one, and why the bundled
    workflows deliberately mix models.
  </Card>
</Cards>

## The short version

The control plane is one binary plus Postgres. Give it a persistent volume, a
stable `HARNESS_SECRET_KEY`, and accounts for whichever agents your workflows
name. Everything else has a working default.
